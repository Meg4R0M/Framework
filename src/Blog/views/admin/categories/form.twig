{{ csrf_input() }}
<div class="card-body">
    <div class="row">
        <div class="col-md-12">
            <div class="form-group">
                {{ field('name', item.name, "Titre de la catégorie") }}
            </div>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-12">
            <div class="form-group">
                {{ field('slug', item.slug, "Lien") }}
            </div>
        </div>
    </div>
</div>