<div class="col-md-3">
    <div class="card p-4">
        <div class="card-body d-flex justify-content-between align-items-center">
            <div>
                <span class="h4 d-block font-weight-normal mb-2">{{ count }}</span>
                <span class="font-weight-light">Compte{% if count > 1 %}s{% endif %}</span>
            </div>

            <div class="h2 text-muted">
                <i class="icon icon-people"></i>
            </div>
        </div>
        <a href="{{ path('account.admin.index') }}" class="btn btn-primary">Gérer les comptes</a>
    </div>
</div>